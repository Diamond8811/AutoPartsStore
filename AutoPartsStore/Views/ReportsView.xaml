<UserControl x:Class="AutoPartsStore.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15" CornerRadius="8" Margin="0,0,0,10">
            <WrapPanel>
                <TextBlock Text="Дата с:" VerticalAlignment="Center" Margin="5,0,2,0"/>
                <DatePicker SelectedDate="{Binding DateFrom}" Margin="5"/>
                <TextBlock Text="по:" VerticalAlignment="Center" Margin="5,0,2,0"/>
                <DatePicker SelectedDate="{Binding DateTo}" Margin="5"/>
                <Button Content="Применить даты" Command="{Binding ApplyDatesCommand}" Margin="5"/>
            </WrapPanel>
        </Border>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button Content="📊 Продажи" Command="{Binding SalesReportCommand}" Margin="5"/>
            <Button Content="📦 Остатки" Command="{Binding StockReportCommand}" Margin="5"/>
            <Button Content="🔄 Возвраты" Command="{Binding ReturnsReportCommand}" Margin="5"/>
            <Button Content="📎 Экспорт в Excel" Command="{Binding ExportReportCommand}" Margin="5"/>
        </WrapPanel>

        <DataGrid Grid.Row="2"
                  Name="ReportGrid"
                  ItemsSource="{Binding ReportData}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#F9F9F9"/>

        <ProgressBar Grid.RowSpan="3" IsIndeterminate="{Binding IsBusy}" Width="200" Height="10"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>