<UserControl x:Class="AutoPartsStore.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,10">
            <Button Content="Добавить" Command="{Binding AddCommand}" Margin="5"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}" Margin="5"/>
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Margin="5"/>
        </WrapPanel>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" Padding="10" CornerRadius="8" Margin="0,0,0,10">
            <WrapPanel>
                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5"/>
                <Button Content="Найти" Command="{Binding SearchCommand}" Margin="5"/>
            </WrapPanel>
        </Border>

        <DataGrid Grid.Row="2" 
          ItemsSource="{Binding Customers}" 
          SelectedItem="{Binding SelectedCustomer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
          AutoGenerateColumns="True" 
          IsReadOnly="True"
          SelectionMode="Single"/>

        <ProgressBar Grid.RowSpan="3" IsIndeterminate="{Binding IsBusy}" Width="150" Height="10"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>