<Window x:Class="AutoPartsStore.Views.EditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование товара"
        Height="450"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CardBackgroundBrush}"
                Padding="20"
                CornerRadius="8">

            <StackPanel>

                <TextBlock Text="Редактирование товара"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                <WrapPanel>

                    <TextBlock Text="Артикул:" Margin="5" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Product.Article}" Width="200"/>

                    <TextBlock Text="Название:" Margin="5" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Product.Name}" Width="250"/>

                    <TextBlock Text="Бренд:" Margin="5" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding Brands}"
                              SelectedItem="{Binding SelectedBrand}"
                              Width="200"/>

                    <TextBlock Text="Категория:" Margin="5" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding SelectedCategory}"
                              Width="200"/>

                    <TextBlock Text="Цена:" Margin="5" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Product.Price}" Width="120"/>

                    <TextBlock Text="Остаток:" Margin="5" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Product.StockQuantity}" Width="120"/>

                </WrapPanel>

            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <Button Content="Сохранить"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource AccentButton}"
                    Margin="5"/>

            <Button Content="Отмена"
                    Click="Cancel_Click"
                    Style="{StaticResource DangerButton}"
                    Margin="5"/>

        </StackPanel>

    </Grid>
</Window>