<UserControl x:Class="AutoPartsStore.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CardBackgroundBrush}" Padding="15" CornerRadius="8" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="🔍 Фильтры" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <WrapPanel>
                    <TextBlock Text="Статус:" VerticalAlignment="Center" Margin="5,0,2,0"/>
                    <ComboBox ItemsSource="{Binding Statuses}" DisplayMemberPath="Name" 
                              SelectedItem="{Binding SelectedStatusFilter}" Width="150" Margin="5"/>

                    <TextBlock Text="Дата с:" VerticalAlignment="Center" Margin="5,0,2,0"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" Margin="5"/>

                    <TextBlock Text="по:" VerticalAlignment="Center" Margin="2,0,2,0"/>
                    <DatePicker SelectedDate="{Binding DateTo}" Margin="5"/>

                    <Button Content="Применить" Command="{Binding FilterCommand}" Margin="5"/>
                    <Button Content="Сбросить" Command="{Binding ResetFilterCommand}" Margin="5"/>
                    <Button Content="📎 Экспорт" Command="{Binding ExportOrderCommand}" Margin="5"/>
                    <Button Content="➕ Создать заказ" Command="{Binding CreateOrderCommand}" Margin="5"/>
                    <Button Content="✅ Изменить статус" Command="{Binding ChangeStatusCommand}" Margin="5"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" 
                  SelectedItem="{Binding SelectedOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding OrderId}" Width="60"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Customers.FullName}" Width="200"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding OrderDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding OrderStatuses.Name}" Width="150"/>
                <DataGridTemplateColumn Header="Новый статус" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.Statuses, RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="Name"
                                      SelectedItem="{Binding DataContext.SelectedOrderStatus, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                      Width="140"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" Padding="15" CornerRadius="8" Margin="0,10,0,0"
                Visibility="{Binding SelectedOrder, Converter={StaticResource BoolToVisibility}}">
            <StackPanel>
                <TextBlock Text="🧾 Состав заказа" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <DataGrid ItemsSource="{Binding OrderItems}" AutoGenerateColumns="False" Height="180" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Товар" Binding="{Binding Products.Name}" Width="*"/>
                        <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding UnitPrice, StringFormat=N2}" Width="100"/>
                        <DataGridTextColumn Header="Сумма" Width="100">
                            <DataGridTextColumn.Binding>
                                <MultiBinding Converter="{StaticResource MultiplyConverter}">
                                    <Binding Path="Quantity"/>
                                    <Binding Path="UnitPrice"/>
                                </MultiBinding>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <ProgressBar Grid.RowSpan="3" IsIndeterminate="{Binding IsBusy}" Width="150" Height="10"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>