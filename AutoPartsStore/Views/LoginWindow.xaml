<Window x:Class="AutoPartsStore.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AutoPartsStore.Infrastructure"
        Title="Вход в систему" Height="400" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Авторизация" FontSize="24" FontWeight="Bold"
                       Foreground="#F5F5F5" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBlock Text="Логин" FontSize="12" Margin="5,0,5,0" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 5" ToolTip="Введите логин"/>
            <TextBlock Text="Пароль" FontSize="12" Margin="5,10,5,0" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
            <PasswordBox Margin="0 5" ToolTip="Введите пароль"
                         local:PasswordBoxHelper.BindPassword="True"
                         local:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Войти" Command="{Binding LoginCommand}"
                    Margin="0 20" HorizontalAlignment="Center" Width="120"/>
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</Window>