<UserControl x:Class="AutoPartsStore.Views.ReturnsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="🔄 Обновить" Command="{Binding RefreshCommand}" 
                HorizontalAlignment="Right" Margin="0,0,0,10" Width="120"/>
        
        <Button Grid.Row="0" Content="➕ Оформить возврат" Command="{Binding CreateReturnCommand}" 
        HorizontalAlignment="Left" Margin="0,0,0,10" Width="150"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Returns}" SelectedItem="{Binding SelectedReturn}"
                  AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№ возврата" Binding="{Binding ReturnId}" Width="80"/>
                <DataGridTextColumn Header="№ заказа" Binding="{Binding OrderId}" Width="80"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding ReturnDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Причина" Binding="{Binding ReturnReasons.Reason}" Width="250"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding RefundAmount, StringFormat=N2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <ProgressBar Grid.RowSpan="2" IsIndeterminate="{Binding IsBusy}" Width="150" Height="10"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>