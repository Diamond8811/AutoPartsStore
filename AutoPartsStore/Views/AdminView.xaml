<UserControl x:Class="AutoPartsStore.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CardBackgroundBrush}" Padding="15" CornerRadius="8" Margin="0,0,0,10">
            <WrapPanel HorizontalAlignment="Right">
                <Button Content="🔄 Обновить" Command="{Binding RefreshCommand}" Margin="5"/>
                <Button Content="🔒 Блокировать/Разблокировать" Command="{Binding ToggleBlockCommand}" Margin="5"/>
                <Button Content="🔑 Сбросить пароль" Command="{Binding ResetPasswordCommand}" Margin="5"/>
            </WrapPanel>
        </Border>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Users}" 
                  SelectedItem="{Binding SelectedUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="False"
                  AlternatingRowBackground="#F9F9F9"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="120"/>
                <DataGridTextColumn Header="Полное имя" Binding="{Binding FullName}" Width="200"/>
                <DataGridTemplateColumn Header="Роль" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.Roles, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="Name"
                                      SelectedValue="{Binding RoleId}"
                                      SelectedValuePath="RoleId"
                                      Width="120"
                                      IsEnabled="False"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="💾 Сохранить роль" Command="{Binding SaveRoleCommand}" Margin="5"/>
        </StackPanel>

        <ProgressBar Grid.RowSpan="3" IsIndeterminate="{Binding IsBusy}" Width="150" Height="10"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>